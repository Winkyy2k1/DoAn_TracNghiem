@using TracNghiemOnline.Models
@model LoginModel
@{ ViewBag.Title = "Đăng Nhập Hệ Thống Trắc Nghiệm Online"; }

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        @ViewBag.Title
    </title>
   
    @Scripts.Render("~/bundles/jquery")
    <script src="/Assets/js/materialize.min.js"></script>
</head>

<body class="bg-login">
   
    @*<div id="status" class="status"></div>
    <div class="fade">
        <div class="login fadeInLogin">
            <h5 class="title-login">
                Đăng Nhập
            </h5>
            <div class="login-form">
                <div class="row" style="position: relative;">
                    @using (Html.BeginForm("Index", "Login", FormMethod.Post))
                    {

                        if (ViewBag.error != null)
                        {
                            <span class="title failed">@ViewBag.error</span> }
                        <div class="input-field">
                            @Html.TextBoxFor(m => m.Username, new { @class = "form-control", @required = "required", @autofocus = "autofocus", @placeholder = "Tên đăng nhập" })
                            <label for="username">Tên đăng nhập </label>
                    </div>
                                 <div class="input-field">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", @required = "required", @placeholder = "Mật khẩu" })

                    <label for="password">Mật Khẩu</label>
                </div>
                                <div class="login-btn">
                                    <button type="submit" class="btn waves-effect waves-light ">Đăng Nhập &#10159; </button>
                                </div>}
                </div>
            </div>
        </div>
    </div>*@

    <div class="container" style="margin-top:50px; margin-left: 40%">
        <div class="row vertical-offset-100">
            <div class="col-md-5 col-md-offset-5">
                <div class="panel-heading">
                    <h3 class="panel-title"> Đăng nhập vào tài khoản</h3>
                </div>
                <div class="panel-body">
                    @using (Html.BeginForm("Index", "Login", FormMethod.Post))
                    {
                            if (ViewBag.error != null)
                    {
                        <span class="title failed" style="color: red;">@ViewBag.error </span> }

                        <div class="form-group" style="margin-top:10px">
                            @Html.TextBoxFor(m => m.Username, new { @class = "form-control", @required = "required", @autofocus = "autofocus", @placeholder = "Tên đăng nhập" })
                            @*<input class="form-control" placeholder="Tên đăng nhập" name="email" type="text">*@
                        </div>
                            <div class="form-group">
                                @Html.PasswordFor(m => m.Password, new { @class = "form-control", @required = "required", @placeholder = "Mật khẩu" })
                                @*<input class="form-control" placeholder="Mật khẩu" name="password" type="password" value="">*@
                            </div> 
                            @*<input class="btn btn-lg btn-success btn-block" type="submit" value="Đăng nhập">*@
                        <div class="login-btn">
                            <button type="submit" class="btn" style="background-color: #17a2b8; border-color: #17a2b8; color: #fff; ">Đăng Nhập &#10159; </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

</body>

</html>


